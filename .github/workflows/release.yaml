---
name: Release

on:
  workflow_run:
    workflows:
      - Run Tests
    types:
      - completed


jobs:
  release:
    runs-on: ubuntu-latest
    if: github.ref_type == "tag"
    steps:
      - uses: actions/checkout@v3
      - name: release
        run: |

          docker build . \
            --target release
